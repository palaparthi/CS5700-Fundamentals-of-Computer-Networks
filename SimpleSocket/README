APPROACH

First the program takes the command line arguments and checks the number of arguments passed.
Second the socket is created and connection is made.
Next the Hello message is passed to the server and mathematical expresions are received in response.
Later, the expressions are executed in a loop until a BYE is sent from the server.
Finally, a secret flag is sent from the server with BYE which is prnted.

